def diff_files(left, right, diff_options=None, formatter=None):
    """Takes two streams or streams, and diffs the files in those files"""
    from. import diff_files
    return diff_files.diff_files(left, right, diff_options, formatter)

