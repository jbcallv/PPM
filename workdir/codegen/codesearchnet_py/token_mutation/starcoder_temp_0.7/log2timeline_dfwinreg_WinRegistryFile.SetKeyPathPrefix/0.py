def SetKeyPathPrefix(self, key_path_prefix):
    """Sets the Window key key path prefix.

    Args:
      key_path_prefix (str): Windows - key path prefix.
    """
    self._key_path_prefix = key_path_prefix

