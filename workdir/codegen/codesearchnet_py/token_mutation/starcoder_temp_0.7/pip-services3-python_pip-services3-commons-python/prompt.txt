def name():
        """
        Generates a first person's name which has the following structure
        <optional prefix> <first name> <second name> <optional suffix>

        :return: a particular name.
        """