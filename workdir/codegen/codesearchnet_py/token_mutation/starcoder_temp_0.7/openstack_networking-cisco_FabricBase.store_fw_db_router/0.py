def store_fw_db_router(self, tenant_id, net_id, subnet_id, router_id,
                           os_status):
        """Store the use of FW result operation in DB.

        ##m the service object routine to commit the result of router
        operation in to DB, after updating the local cache.
        """
        pass

